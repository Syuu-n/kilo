import { makeStyles, createStyles } from '@material-ui/core';

const richTableActionsStyle = makeStyles(() =>
createStyles({
  actions: {
    display: 'flex',
  },
}));

export default richTableActionsStyle;
